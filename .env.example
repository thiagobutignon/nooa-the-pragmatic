### General configuration

# Database path used by nooa cron, embeddings, telemetry, and other features.
NOOA_DB_PATH=nooa.db

# Optional: choose the default branch for PR helpers (used when --base is omitted).
NOOA_PR_BASE=main

### AI provider (prompt + fix/review/ci)

# Provider name (e.g., ollama or mock)
NOOA_AI_PROVIDER=ollama

# Endpoint for the AI provider
NOOA_AI_ENDPOINT=http://localhost:11434

# Default model for AI completions
NOOA_AI_MODEL=qwen2.5-coder:14b
# Optional sampling parameter (OpenAI-compatible providers)
NOOA_AI_TOP_P=1

# OpenAI-compatible providers (OpenAI / NVIDIA / others)
# If NOOA_AI_PROVIDER=openai, these values are used by the OpenAI provider.
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1
# Optional request timeout in milliseconds for OpenAI-compatible providers
OPENAI_TIMEOUT_MS=30000
# NVIDIA GLM thinking controls (sent as chat_template_kwargs when set)
OPENAI_ENABLE_THINKING=
OPENAI_CLEAR_THINKING=

# Optional convenience alias for NVIDIA Integrate API.
# When set, it can be used instead of OPENAI_API_KEY.
NVIDIA_API_KEY=

### Embedding provider (used by `nooa embed` and search)

# Provider name (ollama, mock, etc.)
NOOA_EMBED_PROVIDER=ollama

# Embedding model name
NOOA_EMBED_MODEL=nomic-embed-text

# Base URL for the embedding provider
NOOA_EMBED_ENDPOINT=http://localhost:11434

### Search helpers

# Force the search engine (rg or native)
NOOA_SEARCH_ENGINE=rg

# Override the default max results for search commands
NOOA_SEARCH_MAX_RESULTS=100

### Eval history overrides (optional)

# Path to the eval history file
NOOA_EVAL_HISTORY=./.nooa/eval-history.json

# Preseed history data (used only in tests)
NOOA_EVAL_HISTORY_DATA=

### Worktree helpers

# Segregate worktree installs/tests when running CLI tests
NOOA_SKIP_INSTALL=1
NOOA_SKIP_TEST=1

### Misc toggles

# Disable reflection telemetry when running local dev
NOOA_DISABLE_REFLECTION=0

### MCP helpers

# Context7 marketplace API key (used by `nooa mcp marketplace`)
CONTEXT7_API_KEY=

# Global `.mcp.env` file loaded before project-level defaults. Set this only when you want
# a shared secret file across repositories.
# Default: ~/.nooa/mcp.env
NOOA_MCP_GLOBAL_ENV_FILE=~/.nooa/mcp.env

# `.mcp.env` files are loaded automatically by `nooa mcp configure --env-file <path>` (default: `.mcp.env`).
